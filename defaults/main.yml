---
# variable: True
FRESHRSS_PUID: 10001 # TODO
FRESHRSS_PGID: 10001 # TODO
FRESHRSS_HTTP_PORT: 8080 # TODO
FRESHRSS_HTTPS_PORT: 8081 # TODO

FRESHRSS_PATH: '/mnt/freshrss/'
FRESHRSS_STORAGE_LVM_ZFS: 'ZFS' # or 'LVM'
FRESHRSS_STORAGE_ZFS_POOL: 'apps_pool'
FRESHRSS_STORAGE_ZFS_FS: 'freshrss'
#FRESHRSS_STORAGE_ZFS_QUOTA: '0'
FRESHRSS_TIMEZONE: "America/New_York"


FRESHRSS_BACKEND: 'DOCKER' # or 'PODMAN'
FRESHRSS_PODMAN_SERVICE_ACCOUNT: 'srv_freshrss'
FRESHRSS_CONTAINER_VERSION: 'latest'

FRESHRSS_FQDN: 'freshrss.domain.com'

FRESHRSS_BACKUPS_BORG_CONFIGURE: 'True'
FRESHRSS_BACKUPS_FS_SNAPSHOTS_CONFIGURE: 'True'
# Vault variables
## VAULT_FRESHRSS_BACKUPS_BORG_ENCRYPTION_KEY


# TODO:
# FRESHRSS_SSL_CERT_PATH
# FRESHRSS_SSL_KEY_PATH